/**
 * @m199/db — Database package source entry point.
 *
 * The Prisma schema and config are the primary artifacts. This source file
 * provides a minimal export surface so the test baseline can validate the
 * package without requiring a live database connection.
 */

/** Package version identifier used by the test baseline. */
export const DB_PACKAGE_VERSION = "0.0.0" as const;
