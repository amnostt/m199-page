/**
 * Non-product application shell for the Misión 1-99 MVP baseline.
 *
 * This component exists solely to confirm that the Vite React workspace
 * builds and runs. Product UI, routing, auth screens, layout components,
 * and feature views are deferred to later SDD changes.
 */

export function App() {
  return (
    <main>
      <p>Misión 1-99 workspace shell is running.</p>
      <p>
        Product UI ships in future changes. No admin screens, public routes,
        auth flows, uploads, or data features are implemented yet.
      </p>
    </main>
  );
}
