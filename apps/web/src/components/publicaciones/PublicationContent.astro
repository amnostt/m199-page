---
import { sanitizePublicationContentForSsr } from "../../lib/server/publication-sanitizer.js";

interface Props {
  content: string;
}

const { content } = Astro.props;
---

<div class="public-publication-detail__content" set:html={sanitizePublicationContentForSsr(content)} />
